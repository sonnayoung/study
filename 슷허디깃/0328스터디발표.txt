1. 변수 선언하기(let)
 - let은 뭔가 두려운 이미지 
 - let, var , const 
 - let empty; 
let empty; 이렇게연속으로 하면 에러남

2. 변수 이름짓기 
 - 변수명 지을때 특수문자는 $(달러) _(언더스코어) 두개만 가능
 - 숫자는 첫글자로는 안된다. 
 - 앞글자를 대문자로 안함. 카멜형식을 더 많이씀  
 - 예약어 안됨.
 
3. 변수 수정하기
 - num = num + 5 // num =+ 5 // 헷갈려.. (-= , +=, /=,  *=, **=)
 - 중복되는 것들을 변수로 사용하면 편합니다.

4. 상수(const)와 var
 - 상수는 변하지않는수 (constant) : 바꾸려고하면 오류가 나는데 오류를 알고있자.
 - variable(var)는 요새잘 안쓴다고함..와이..충격

5. 조건문 (if)

6. else, else if, switch
 - switch(조건식){
       case 비교조건식:
       동작문 ;
       break;
  } //조건식과 비교조건식이 같으면 동작문실행, 
  - 특이한점은  비교조건식이 여러개일때 그 비교조건식에 맞으면 그 아래 동작문도 다 실행한다. 그럴때 break;를 걸어주면됨
  - else와 같은 작동을 하는거는 default 사용. 가장위에 위치해도됨
  default: 
      동작문; 

7. 조건부 연산자 (삼항 연산자)
 - 조건식 ? 참일때실행 : 거짓일때 실행;
 - 2021년버전 ?.  ?? 가 있는데 나중에 알려준다함
 - con1 ? (con2 ? '둘다참' : 'con1만참') : 'con1이거짓'; // 삼항연산자안에 삼항연산자 넣으려면 이렇게. 들여쓰기도가능
 




